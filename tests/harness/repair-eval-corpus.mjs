// A diverse broken-repo repair corpus for the model-backed eval. Each fixture is
// a small, self-contained bug in one source file with a failing test that pins
// the intended behavior. The bugs span distinct categories (operator, off-by-one,
// boundary, null-guard, async, comparator, recursion, regex, mutation, type
// coercion, etc.) so pass@k measures genuine repair breadth — not one trick
// repeated. These are NOT templated stubs: each requires reading the test to infer
// intent and making a real, minimal edit.

export const REPAIR_FIXTURES = [
  { id: "operator-add", category: "wrong-operator",
    src: "export const f = (a, b) => a - b;\n",
    test: "import {f} from '../src.mjs';\nt('add', () => eq(f(2,3), 5));\n",
    instruction: "f(a,b) should add but subtracts; fix the operator." },
  { id: "offbyone-slice", category: "off-by-one",
    src: "export const lastN = (arr, n) => arr.slice(arr.length - n + 1);\n",
    test: "import {lastN} from '../src.mjs';\nt('lastN', () => eq(lastN([1,2,3,4],2), [3,4]));\n",
    instruction: "lastN returns one too few elements; fix the off-by-one." },
  { id: "boundary-clamp", category: "boundary",
    src: "export const clamp = (x, lo, hi) => x < lo ? lo : x > hi ? hi : x + 1;\n",
    test: "import {clamp} from '../src.mjs';\nt('clamp mid', () => eq(clamp(5,0,10), 5));\n",
    instruction: "clamp mutates in-range values; it must return x unchanged when within [lo,hi]." },
  { id: "null-guard", category: "null-guard",
    src: "export const nameOf = (u) => u.name.toUpperCase();\n",
    test: "import {nameOf} from '../src.mjs';\nt('missing', () => eq(nameOf(null), ''));\n",
    instruction: "nameOf throws on null/undefined; return '' when there is no name." },
  { id: "async-await", category: "async-missing-await",
    src: "export const load = async (fn) => { const v = fn(); return v + 1; };\n",
    test: "import {load} from '../src.mjs';\nt('await', async () => eq(await load(async () => 41), 42));\n",
    instruction: "load does not await fn(); await it before adding." },
  { id: "comparator-sort", category: "comparator",
    src: "export const sortNums = (a) => [...a].sort();\n",
    test: "import {sortNums} from '../src.mjs';\nt('numeric', () => eq(sortNums([10,2,1]), [1,2,10]));\n",
    instruction: "sort() is lexicographic; provide a numeric comparator." },
  { id: "recursion-base", category: "recursion",
    src: "export const fact = (n) => n * fact(n - 1);\n",
    test: "import {fact} from '../src.mjs';\nt('fact', () => eq(fact(5), 120));\n",
    instruction: "fact has no base case (infinite recursion); return 1 when n<=1." },
  { id: "regex-anchor", category: "regex",
    src: "export const isHex = (s) => /[0-9a-f]+/.test(s);\n",
    test: "import {isHex} from '../src.mjs';\nt('reject', () => eq(isHex('zzz12'), false));\n",
    instruction: "isHex matches substrings; anchor the regex to the whole string." },
  { id: "mutation-immut", category: "mutation",
    src: "export const withId = (o, id) => { o.id = id; return o; };\n",
    test: "import {withId} from '../src.mjs';\nt('immutable', () => { const a={}; withId(a,1); eq(a.id, undefined); });\n",
    instruction: "withId mutates its argument; return a new object instead." },
  { id: "type-coerce", category: "type-coercion",
    src: "export const sum = (arr) => arr.reduce((a, b) => a + b);\n",
    test: "import {sum} from '../src.mjs';\nt('strings', () => eq(sum(['1','2','3']), 6));\n",
    instruction: "sum concatenates string inputs; coerce values to numbers." },
  { id: "empty-default", category: "empty-input",
    src: "export const avg = (a) => a.reduce((x,y)=>x+y,0) / a.length;\n",
    test: "import {avg} from '../src.mjs';\nt('empty', () => eq(avg([]), 0));\n",
    instruction: "avg returns NaN on []; return 0 for empty input." },
  { id: "string-trim", category: "string",
    src: "export const slug = (s) => s.toLowerCase().replace(/ /g, '-');\n",
    test: "import {slug} from '../src.mjs';\nt('trim', () => eq(slug('  Hi There '), 'hi-there'));\n",
    instruction: "slug keeps leading/trailing spaces as dashes; trim before slugifying." },
  { id: "dedupe", category: "set-logic",
    src: "export const uniq = (a) => a.filter((x, i) => a.indexOf(x) !== i);\n",
    test: "import {uniq} from '../src.mjs';\nt('uniq', () => eq(uniq([1,1,2,3,3]), [1,2,3]));\n",
    instruction: "uniq returns the duplicates; invert the condition to keep first occurrences." },
  { id: "clone-deep", category: "reference",
    src: "export const copy = (o) => o;\n",
    test: "import {copy} from '../src.mjs';\nt('copy', () => { const a={n:1}; const b=copy(a); b.n=2; eq(a.n,1); });\n",
    instruction: "copy returns the same reference; return a shallow clone." },
  { id: "range-incl", category: "off-by-one",
    src: "export const range = (n) => Array.from({length:n}, (_,i)=>i+1).slice(0,-1);\n",
    test: "import {range} from '../src.mjs';\nt('range', () => eq(range(3), [1,2,3]));\n",
    instruction: "range drops the last element; remove the slice." },
  { id: "max-empty", category: "boundary",
    src: "export const max = (a) => a.reduce((m,x)=> x>m ? x : m, 0);\n",
    test: "import {max} from '../src.mjs';\nt('neg', () => eq(max([-3,-1,-2]), -1));\n",
    instruction: "max seeds the reduce with 0, so all-negative inputs wrongly return 0; seed from the first element instead." },
  { id: "count-pred", category: "predicate",
    src: "export const countEven = (a) => a.filter(x => x % 2).length;\n",
    test: "import {countEven} from '../src.mjs';\nt('even', () => eq(countEven([2,4,5,6]), 3));\n",
    instruction: "countEven counts odds; test for === 0 remainder." },
  { id: "merge-objs", category: "object",
    src: "export const merge = (a, b) => ({ ...b, ...a });\n",
    test: "import {merge} from '../src.mjs';\nt('override', () => eq(merge({x:1},{x:2,y:3}), {x:2,y:3}));\n",
    instruction: "merge lets a override b, but b should win; swap the spread order." },
  { id: "capitalize", category: "string",
    src: "export const cap = (s) => s.charAt(0) + s.slice(1).toUpperCase();\n",
    test: "import {cap} from '../src.mjs';\nt('cap', () => eq(cap('hello'), 'Hello'));\n",
    instruction: "cap upper-cases the tail instead of the first letter; fix it." },
  { id: "flatten", category: "array",
    src: "export const flat = (a) => a.flat(0);\n",
    test: "import {flat} from '../src.mjs';\nt('flat', () => eq(flat([1,[2,[3]]]), [1,2,3]));\n",
    instruction: "flat(0) does nothing; flatten deeply." },
  { id: "includes-ci", category: "string",
    src: "export const has = (h, n) => h.includes(n);\n",
    test: "import {has} from '../src.mjs';\nt('ci', () => eq(has('Hello World','world'), true));\n",
    instruction: "has is case-sensitive; make the search case-insensitive." },
  { id: "debounce-key", category: "object",
    src: "export const groupBy = (a, k) => a.reduce((m,x)=>{ m[x[k]]=x; return m; },{});\n",
    test: "import {groupBy} from '../src.mjs';\nt('group', () => eq(groupBy([{t:'a',v:1},{t:'a',v:2}],'t'), {a:[{t:'a',v:1},{t:'a',v:2}]}));\n",
    instruction: "groupBy overwrites instead of grouping into arrays; push into per-key arrays." },
  { id: "pct", category: "math",
    src: "export const pct = (n, total) => n / total;\n",
    test: "import {pct} from '../src.mjs';\nt('pct', () => eq(pct(1,4), 25));\n",
    instruction: "pct returns a fraction; return a whole percentage (0-100)." },
  { id: "first-truthy", category: "logic",
    src: "export const firstTruthy = (a) => a.find(x => !x);\n",
    test: "import {firstTruthy} from '../src.mjs';\nt('first', () => eq(firstTruthy([0,'',null,5,6]), 5));\n",
    instruction: "firstTruthy finds the first falsy; invert to find the first truthy." },
  { id: "chunk", category: "array",
    src: "export const chunk = (a, n) => { const r=[]; for(let i=0;i<a.length;i+=n) r.push(a.slice(i, i+n-1)); return r; };\n",
    test: "import {chunk} from '../src.mjs';\nt('chunk', () => eq(chunk([1,2,3,4],2), [[1,2],[3,4]]));\n",
    instruction: "chunk slices one short; the end bound should be i+n." },
  { id: "title-case", category: "string",
    src: "export const title = (s) => s.split(' ').map(w => w[0].toUpperCase()+w.slice(1)).join('');\n",
    test: "import {title} from '../src.mjs';\nt('title', () => eq(title('a b c'), 'A B C'));\n",
    instruction: "title joins without spaces; join with a single space." },
  { id: "safe-json", category: "error-handling",
    src: "export const parse = (s) => JSON.parse(s);\n",
    test: "import {parse} from '../src.mjs';\nt('bad', () => eq(parse('{bad'), null));\n",
    instruction: "parse throws on invalid JSON; return null on parse error." },
  { id: "zip", category: "array",
    src: "export const zip = (a, b) => a.map((x,i)=>[x, b[i+1]]);\n",
    test: "import {zip} from '../src.mjs';\nt('zip', () => eq(zip([1,2],[3,4]), [[1,3],[2,4]]));\n",
    instruction: "zip pairs with the wrong index (b[i+1]); use b[i]." },
  { id: "count-words", category: "string",
    src: "export const words = (s) => s.split(' ').length;\n",
    test: "import {words} from '../src.mjs';\nt('words', () => eq(words('  hi   there  '), 2));\n",
    instruction: "words miscounts on extra whitespace; split on /\\s+/ and ignore empties." },
  { id: "is-palindrome", category: "string",
    src: "export const pal = (s) => s === s.split('').reverse();\n",
    test: "import {pal} from '../src.mjs';\nt('pal', () => eq(pal('aba'), true));\n",
    instruction: "pal compares a string to an array; join the reversed array before comparing." },
  { id: "sum-positive", category: "predicate",
    src: "export const sumPos = (a) => a.reduce((s,x)=> s + x, 0);\n",
    test: "import {sumPos} from '../src.mjs';\nt('pos', () => eq(sumPos([-1,2,-3,4]), 6));\n",
    instruction: "sumPos sums everything; only add positive numbers." },
  { id: "default-param", category: "logic",
    src: "export const greet = (name) => `Hi ${name}`;\n",
    test: "import {greet} from '../src.mjs';\nt('default', () => eq(greet(), 'Hi there'));\n",
    instruction: "greet renders 'Hi undefined' with no arg; default name to 'there'." }
];

export function fixtureCount() { return REPAIR_FIXTURES.length; }
